<%= form_with(model: block_list, local: true) do |form| %>
  <% if block_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(block_list.errors.count, "error") %> prohibited this block_list from being saved:</h2>

      <ul>
      <% block_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :blocked_by %>
    <%= form.number_field :blocked_by, id: :block_list_blocked_by %>
  </div>

  <div class="field">
    <%= form.label :blocked %>
    <%= form.number_field :blocked, id: :block_list_blocked %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
