<%= form_with(model: meet_request, local: true) do |form| %>
  <% if meet_request.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(meet_request.errors.count, "error") %> prohibited this meet_request from being saved:</h2>

      <ul>
      <% meet_request.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :meet_request_user_id %>
    <%= form.number_field :meet_request_user_id, id: :meet_request_user_id %>
  </div>

  <div class="field">
    <%= form.label :meet_target_user_id %>
    <%= form.number_field :meet_target_user_id, id: :meet_target_user_id %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
